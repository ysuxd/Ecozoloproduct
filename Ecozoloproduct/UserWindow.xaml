<Window x:Class="WpfApp2.UserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Личный кабинет" 
        Height="450" 
        Width="600"
        WindowStartupLocation="CenterScreen"
        Background="#F5F0E8">

    <Grid Margin="20" Background="#F5F0E8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#5A8F4C" CornerRadius="8" Margin="0,0,0,15">
            <TextBlock Text="👤 ЛИЧНЫЙ КАБИНЕТ" 
                       FontSize="22" 
                       FontWeight="Bold" 
                       Foreground="White" 
                       HorizontalAlignment="Center" 
                       Padding="20,15"
                       FontFamily="Segoe UI"/>
        </Border>

        <!-- Информация о пользователе -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,15"
                BorderBrush="#E8DCC8" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="📋 Информация о пользователе" 
                           FontSize="16" 
                           FontWeight="Bold" 
                           Foreground="#4A3728"
                           Margin="0,0,0,15"/>
                <TextBlock x:Name="UserInfoText" 
                           FontSize="14" 
                           Foreground="#8B9A6E"/>
            </StackPanel>
        </Border>

        <!-- Кнопка выхода -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="20"
                BorderBrush="#E8DCC8" BorderThickness="1">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Button x:Name="ExitButton" Content="🚪 Выход из системы" 
                        Width="200" Height="45" Click="ExitButton_Click"
                        Background="#E8DCC8" Foreground="#4A3728" FontWeight="Bold" Cursor="Hand"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>